# conda environment setup for travis-CI environment
# intended for any python version 3x
name: test-environment
channels:
  - conda-forge
dependencies:
  - python=3.7
  - numpy
  # note netcdf4 rather than netCDF4
  - netcdf4